spring:
  datasource:
    url: jdbc:mysql://mysql:3306/legal_assistant?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_ROOT_PASSWORD:root123456}

  data:
    redis:
      host: redis
      port: 6379

minio:
  endpoint: http://minio:9000

milvus:
  host: milvus
  port: 19530

logging:
  level:
    com.legal.assistant: info
    org.springframework.web: warn
